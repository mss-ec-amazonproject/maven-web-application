---
- name: file module demo
  hosts: all
  becomes: yes
  become_user: root
  vars:
    myfile: "~/data/sample123.txt"
  tasks:
    - name: create directory
      ansible.builtin.file:
        path: ~/data2
        state: directory
    - name: mount partions
      mount:
        path: ~/data2
        src: /dev/xvda1
        fstype: ext4
        state: mounted
     
